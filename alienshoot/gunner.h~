#ifndef __GUNNER_H__
#define __GUNNER_H__

#include "bounded_sprite.h"
#include "sound_bank.h"


class gunner : public bounded_sprite
{
 public: 
  
   gunner( display_manager * dm, SDL_Rect bounds);  
  ~gunner(); 

  void move( int howfar );
  int get_y(); 
  int get_x(); 
  void reset();

 protected:
  int _defaultx;
  int _defaulty;
  sound_bank * _shot_sound;
  
    
};


#endif
